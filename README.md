This is a house price predictor project as I was learning , initially I used RandomForestRegressor but after I completed it and tuned the hyperparameters it still gave average results.
So , I used XGBoost as it is considered to be far better than RandomForestRegressor in tabular data predicting.
